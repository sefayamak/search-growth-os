# Deployment providers

State today: Vercel team contains a project named `pamistanbul` (FACT, listed 2026-09-20); its linked repository and framework are UNKNOWN until inspected with the owner.

Usage: read deployment timestamps (forensics), preview URLs (release regression), and log drains. The system never triggers a deployment; merges are human.
