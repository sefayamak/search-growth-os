# Bing Webmaster Tools — connection

State today: **NOT_CONNECTED**.

1. Verify the site in Bing Webmaster Tools (import from Search Console is the fastest path).
2. Generate an API key (Settings → API access); set `SEARCH_GROWTH_BING_API_KEY`.
3. Set `bing_webmaster_property`.
4. Data used: query/page stats, crawl stats and errors, index explorer, and the AI Performance (Copilot) report where available — confirm availability at connect time and record it in `policies/references/official-sources.md`.
